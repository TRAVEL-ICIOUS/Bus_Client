@model Bus_Client.ServiceReference1.Insert_BusInfo
@{
    ViewBag.Title = "GetBusInfo";
}

<h2 style="text-align:center">GetBusInfo</h2>

@using (Html.BeginForm())
{
    <table>
        <tr>
            <td>BusID</td>
            <td>@Html.TextBoxFor(x => x.BusID)</td>
        </tr>

        <tr>
            <td>Name</td>
            <td>@Html.TextBoxFor(x => x.BusName)</td>
        </tr>

        <tr>
            <td>Type</td>
            <td>@Html.TextBoxFor(x => x.BusType)</td>
        </tr>

        <tr>
            <td>OG_Capacity</td>
            <td>@Html.TextBoxFor(x => x.BusOrgCapacity)</td>
        </tr>

        <tr>
            <td></td>
        <td>
            <input type="submit" />
            </td>
        </tr>
    </table>
    if (ViewBag.msg != null)
    {

        @ViewBag.msg



    }
}
<style>
    table {
        justify-content: center;
        text-align: center;
        align-content: center;
        position: center;
        top: 0;
        bottom: 0;
        right: 0;
        left: 0;
        margin: auto;
    }

    text {
        text-align: center;
        width: 100%;
        background-color: white;
        color: black;
        font-weight: bold;
        border-radius: 20px;
    }

    td {
        font-size: 16px;
        font-weight: bold;
    }

    input {
        margin-top: 10px;
        text-align: center;
        width: 100%;
        padding: 10px 20px;
        background-color: white;
        color: black;
        font-weight: bold;
        font-size: 20px;
        border-radius: 20px;
    }

        input.btn {
            margin-top: 10px;
            text-align: center;
            width: 100%;
            padding: 10px 20px;
            background-color: lightgrey;
            color: black;
            font-weight: bold;
            font-size: 20px;
            border-radius: 20px;
        }
</style>

